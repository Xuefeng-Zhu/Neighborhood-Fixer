# Provider API verification record

Checked 2026-09-13/14 against official documentation and installed pinned packages. The contract references below are separate from the authorized live verification results recorded after the table.

The current source is migrating to Clerk. The previous identity-provider live evidence below is historical and does not prove Clerk deployment, signup, shared admission, token renewal or post-reset correctness. Those require fresh live acceptance before public admission. Local migration checks passed the 167-test Python suite (four explicit live-AWS skips), 29 frontend tests, 11 hosted-helper tests, two local Chromium journeys and 15 CDK assertions. Both synth variants passed (45 Clerk-only resources; 48 with temporary legacy retention), without cloud mutations. See [cutover sequence](aws-deployment.md).

| Area | Implemented contract | Evidence |
|---|---|---|
| Clerk default session | Exact development issuer, custom aud=neighborhood-fixer-api and scope=nf:resident, retaining ordinary session binding; no JWT template or frontend secret | [Session customization](https://clerk.com/docs/guides/sessions/customize-session-tokens) |
| API authorization | Generic HTTP API JWT authorizer with exact issuer/audience and nf:resident route scope; backend checks trusted azp/session/expiry plus server admission | [HTTP API JWT authorization](https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api-jwt-authorizer.html) |
| Preflight | Explicit public OPTIONS route and public GET health; exact frontend origin and Authorization/Content-Type/Idempotency-Key headers | [HTTP API CORS](https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api-cors.html) |
| Strands structured output | Agent `structured_output_model=...`; consume `AgentResult.structured_output`, no deprecated extraction API | [Strands structured output](https://strandsagents.com/docs/user-guide/concepts/agents/structured-output/) |
| Tool and model bounds | `BeforeModelCallEvent`, `BeforeToolCallEvent`, `AfterToolCallEvent`; bounded GraphBuilder node count and deadlines | [Strands hooks](https://strandsagents.com/docs/user-guide/concepts/agents/hooks/) and installed 1.55.1 signatures |
| Multimodal provider | `us.amazon.nova-2-lite-v1:0` in `us-west-2`; images and client-side tools; US geographic inference | [Nova 2 Lite model card](https://docs.aws.amazon.com/bedrock/latest/userguide/model-card-amazon-nova-2-lite.html), [Nova 2 guide](https://docs.aws.amazon.com/nova/latest/nova2-userguide/what-is-nova-2.html) |
| Runtime packaging | ARM64 Linux container, port 8080, `BedrockAgentCoreApp` entrypoint, HTTP invocation contract | [Runtime HTTP contract](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/runtime-http-protocol-contract.html), [Runtime hosting](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/agents-tools-runtime.html) |
| Runtime invocation | boto3 `bedrock-agentcore.invoke_agent_runtime`, IAM auth, fresh 36-character UUID runtime session per request | [Runtime invocation](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/runtime-invoke-agent.html) |
| Browser | `BrowserClient.start(identifier=...,session_timeout_seconds=...)`, `generate_ws_headers`, Playwright `connect_over_cdp`, `stop` in finally | [Session management](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/browser-managing-sessions.html), [Playwright quickstart](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/browser-quickstart-playwright.html) |
| Region intersection | Oregon supports Runtime and Browser; model uses geographic inference | [AgentCore regions](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/agentcore-regions.html) |
| CDK | Actual `CfnRuntime`, `CfnBrowserCustom` CloudFormation resources, not printing deployment placeholders | [CfnRuntime](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_bedrockagentcore.CfnRuntime.html), [CfnBrowserCustom](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_bedrockagentcore.CfnBrowserCustom.html) |
| Hosting origin | Generated Amplify App `DefaultDomain` supplies the first-deployment origin; Vite settings belong to the branch to avoid a resource dependency cycle | [Amplify App](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-amplify-app.html), [Amplify Branch](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-amplify-branch.html) |
| Deployment credentials | Optional `CliCredentialsStackSynthesizer` with dedicated retained S3/ECR asset stack; no persistent deployment roles, no bootstrap SSM requirement | [CDK synthesizer](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.CliCredentialsStackSynthesizer.html) |
| Lambda concurrency | Worker reservation omitted by default; explicit positive opt-in after checking account quota | [Lambda concurrency](https://docs.aws.amazon.com/lambda/latest/dg/configuration-concurrency.html) |
| Durable orchestration | Standard workflow; Lambda `.waitForTaskToken`, server-side callback and explicit bounded waits | [Workflow types](https://docs.aws.amazon.com/step-functions/latest/dg/choosing-workflow-type.html), [Integration patterns](https://docs.aws.amazon.com/step-functions/latest/dg/connect-to-resource.html) |
| Atomic reservation | Strong reads plus conditional TransactWriteItems; application operation identifiers | [DynamoDB transactions](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/transaction-apis.html) |
| Location | MapLibre with map-only, map-scoped, referrer-restricted expiring Amazon Location key. Reverse-geocoding guidance was reviewed, but no reverse-geocoding endpoint is implemented; coordinates remain resident-confirmed | [Reverse geocoding](https://docs.aws.amazon.com/location/latest/developerguide/reverse-geocode.html), [Location API keys](https://docs.aws.amazon.com/location/latest/developerguide/using-apikeys.html) |

Pinned versions: `strands-agents==1.55.1`, `bedrock-agentcore==1.23.0`, `boto3==1.43.93`, `aws-cdk-lib==2.269.0`, `aws-cdk==2.1141.0`, `constructs==10.8.1`. Package releases were checked against the official PyPI/npm registries; compatible dependencies were installed and lockfiles generated.

Historical pre-migration local results: 106 Python tests passed with four opt-in live smoke tests skipped and three warnings in 15.91 seconds, including deterministic Strands graph and Browser connection regressions. Twelve CDK assertions and nine frontend AWS-helper checks passed. That main synthesis had 48 resources excluding CDK metadata; the optional asset stack had four and no IAM role. Both ARM64 images built and their local startup contracts passed. Asset contexts contain only allowlisted source/fixtures/build files, excluding credentials, private local data and caches. These deterministic results do not substitute for live checks of later changes.

Historical authorized live deployment completed in account ending 4085, us-west-2. The backend update reached UPDATE_COMPLETE at 08:05:46 UTC on 2026-09-14; health checks returned HTTP 200 at 08:07 UTC. Two separate API/storage tests passed. Both synthetic residents passed the previous hosted login, API, disabled local controls, Location styles/tiles/attribution, refresh and logout. That account setup is not migrated to Clerk and does not establish current owner sign-in.

The initial AWS account-verification gate cleared at 07:31 UTC on 2026-09-14. Real Runtime routing passed after the 07:49 UTC deployment with exact configured registry constraints. The final live API journey verified two residents' synthetic-photo analyses through private S3, Step Functions and Runtime, with real Bedrock/Strands provenance and successful `read_authorized_evidence` and `query_nearby_incidents` activity. The accepted analyses recorded 3 and 4 tool activities and retained 3 unknowns each. This establishes the deployed image-read and orchestration path. Earlier incorrect blocking clarification about unknown dimensions, safety and ownership, followed by a draft tool-guard failure, were corrected before the final continuation. The separate earlier inline-photo Runtime attempt remains `needs_review` with `CHECK_FAILED` and supplies no independent success evidence.

The final live continuation passed through exact registry routing and coordinator preparation, producing one canonical case, two observations and one immutable draft payload hash. Resident B could not read A's private photo or draft. A's inspected existing photo and completed analysis were reused after prior-operation and observation-input checks; no duplicate A observation or new A photo analysis was created. B's first upload and analysis linked to that same canonical case without changing the draft hash. The private report `.local/aws-draft-tools-reviewed-report.json` ended with status `paused_before_approval`: zero approval requests, no Browser submission and no agency closure. The persisted case and draft require review before any further action; the driver must not be blindly replayed. Approved attachment transfer, receipt, status polling, durable closure and resident verification remain unverified in AWS.

Custom and built-in AgentCore Browser sessions remain READY/ENABLED but return automation-stream HTTP 404 before page navigation, including after account verification cleared. Browser-start and stream-signing credentials were compared in memory and matched; no IAM misconfiguration has been established. The stream failure remains unresolved. The fictional portal itself returns HTTP 200, establishing reachability only. See [current deployment status](AWS-STATUS.md) for the handoff and subsequent results.

The deployment corrected two observed integration failures: Runtime now depends on its attached ECR policy, and API Gateway has an explicitly unauthenticated OPTIONS route while private requests remain JWT protected. Browser discovery has bounded same-session retries and redacted diagnostic counters; it never repeats page/form actions. [HTTP API CORS](https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api-cors.html), [Browser connection troubleshooting](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/browser-tool-troubleshooting.html).

A default-disabled cloud operator path is provided for fictional agency status changes: `DemoStatusManagementEnabled=true` enables the portal endpoint only in AWS demo mode with the existing service-secret check. The operator CLI derives its destination and secret ARN from trusted stack outputs, validates the portal mode, and sends one confirmed update. AWS identity switching, reset and virtual-clock controls remain disabled. This operator path, the subsequent real-time status polling, and resident verification still require live deployment validation.
