# Progress

- Complete locally: persisted API, restartable worker, fictional agency portal, two-resident case flow, exact approval, browser submission, receipt recovery, agency status, resident verification, concurrency, privacy, and desktop/mobile UI checks.
- Complete in AWS: Strands/Bedrock, private DynamoDB/S3, Step Functions, AgentCore Runtime, Amplify, Amazon Location, scoped IAM, health, and CORS in `us-west-2`.
- Complete authentication cutover: the live app uses Clerk issuer `https://working-turtle-1775.clerk.accounts.dev`, audience `neighborhood-fixer-api`, and scope `nf:resident`. Public signup, authenticated API, token renewal, refresh, logout, wrong-token rejection, and server-selected workspace membership passed.
- Complete identity cleanup: CloudFormation contains zero Cognito resources and the exact former user pool was deleted. Clerk contains zero test users and zero allowlist entries, with its temporary allowlist restriction disabled.
- Complete map acceptance: the live Amplify page loaded the Amazon Location style and tiles, rendered the map canvas, and displayed attribution using the restricted map key.
- Complete real AI validation: two residents' private photos ran through S3, Step Functions, AgentCore Runtime, and Bedrock/Strands; both observations linked to one canonical case and one immutable draft while cross-resident photo and draft access stayed denied.
- Complete quota validation: the tenth report reached 10/10, request eleven returned 429, and an idempotent replay returned its original 201 without another charge.
- Complete reset and public opening: generation `clerk-public-v1` is public with one inert sample incident and observation, zero QA identities, zero operations/tickets/approvals/portal records, and no evidence objects or uploads. A fresh public signup verified the clean state and was deleted afterward.
- Complete release checks: 182 Python tests passed with 4 explicit live-AWS skips and 3 dependency warnings; 29 frontend tests, 2 local Playwright journeys against Neighborhood Fixer on port 5174, 11 hosted-helper tests, and 14 CDK assertions passed; web typecheck/build and CDK build/synthesis passed. Vite's large-chunk advisory is non-blocking.
- Remaining cloud limitation: AgentCore Browser custom and built-in sessions report READY/ENABLED, but signed CDP connections return HTTP 404 before navigation. Remote submission, attachment transfer, receipt, status polling, closure, and resident verification are therefore unverified. No agency submission was made.
- Live endpoints: [application](https://main.d12il66dljooo6.amplifyapp.com/) and [API](https://09iviho9v3.execute-api.us-west-2.amazonaws.com). See [AWS status](AWS-STATUS.md) and the [test report](TEST-REPORT.md).
